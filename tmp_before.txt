<?php
require_once("../../auth.php");
require_login('../../initial_screen_group/php/login.php');

// 仮ログイン（ログイン後は不要）
// ログイン済みのため $_SESSION['user_id'] を利用

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $nickname = isset($_POST['user_name']) ? trim($_POST['user_name']) : (isset($_POST['nickname']) ? trim($_POST['nickname']) : '');
    if ($nickname !== '') {
        $stmt = $pdo->prepare("UPDATE users SET user_name = :user_name WHERE user_id = :id");
        $stmt->bindValue(":user_name", $nickname, PDO::PARAM_STR);
        $stmt->bindValue(":id", $_SESSION['user_id'], PDO::PARAM_INT);
        $stmt->execute();
        header("Location: profile.php");
        exit;
    }
}
?>
<?php
// 現在のユーザー名をDBから取得
$stmt = $pdo->prepare("SELECT user_name FROM users WHERE user_id = :id");
$stmt->bindValue(":id", $_SESSION['user_id'], PDO::PARAM_INT);
$stmt->execute();
$row = $stmt->fetch(PDO::FETCH_ASSOC) ?: ['user_name' => ''];
$db_nickname = $row['user_name'];
$current_nickname = htmlspecialchars($row['user_name']);
?>



<?php
// 仮ユーザー情報（本来はDBから取得）
$user = [
    'nickname' => '現在のニックネーム'
];
?>
<?php $user = ['nickname' => $db_nickname ?? '']; ?>
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ニックネーム変更</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: #f4f4f8;
            margin: 0;
            padding: 0;
        }

        .app-container {
            max-width: 420px;
            margin: 0 auto;
            background: #fff;
            min-height: 100vh;
            position: relative;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            padding-bottom: 80px;
        }

        /* --- ヘッダー --- */
        .header {
            display: flex;
            align-items: center;
            padding: 1rem;
            border-bottom: 1px solid #eee;
            background: #fff;
        }

        .back-btn {
            font-size: 1.5rem;
            text-decoration: none;
            color: #333;
            margin-right: 10px;
        }

        .header-title {
            font-weight: bold;
            font-size: 1rem;
        }

        /* --- プロフィールリスト --- */
        .info-box {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem;
            border-bottom: 1px solid #f0f0f0;
            font-size: 0.95rem;
        }

        .info-box label {
            color: #555;
        }

        .info-box .value {
            color: #333;
        }

        /* --- モーダル --- */
        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,0.4);
            display: flex;
            justify-content: center;
            align-items: flex-end;
        }

        .modal {
            width: 100%;
            max-width: 420px;
            background: #fff;
            border-top-left-radius: 20px;
            border-top-right-radius: 20px;
            padding: 1.5rem;
            animation: slideUp 0.3s ease-out;
        }

        @keyframes slideUp {
            from { transform: translateY(100%); }
            to { transform: translateY(0); }
        }

        .drag-bar {
            width: 40px;
            height: 4px;
            background: #ccc;
            border-radius: 2px;
            margin: 0 auto 10px;
        }

        .modal h3 {
            text-align: center;
            font-size: 1rem;
            margin-bottom: 1rem;
            font-weight: bold;
        }

        .modal input {
            width: 100%;
            padding: 0.7rem;
            border: 1px solid #ddd;
            border-radius: 6px;
            margin-bottom: 1rem;
            font-size: 0.9rem;
        }

        .modal-buttons {
            display: flex;
            justify-content: space-between;
            gap: 0.5rem;
        }

        .cancel-btn, .confirm-btn {
            flex: 1;
            padding: 0.7rem;
            border-radius: 8px;
            font-weight: bold;
            border: none;
            cursor: pointer;
        }

        .cancel-btn {
            background: #fff;
            border: 1px solid #ff7b7b;
            color: #ff7b7b;
        }

        .confirm-btn {
            background: #ff7b7b;
            color: #fff;
        }
    </style>
</head>
<body>
<div class="app-container">
    
    <!-- ヘッダー -->
    <div class="header">
        <a href="###" class="back-btn">←</a>
        <div class="header-title">ニックネーム</div>
    </div>

    <!-- 情報表示 -->
    <div class="info-box">
        <label>性別</label>
        <div class="value">男性</div>
    </div>
    <div class="info-box">
        <label>体重</label>
        <div class="value">65kg</div>
    </div>

    <!-- モーダル -->
    <div class="modal-overlay">
        <div class="modal">
            <div class="drag-bar"></div>
            <h3>ニックネーム変更</h3>

            <form method="POST" action="nickname_setting.php">
                <input type="text" name="nickname" placeholder="ニックネームを入力してください" value="<?= htmlspecialchars($user['nickname']) ?>">
                <div class="modal-buttons">
                    <button type="button" class="cancel-btn" onclick="history.back()">キャンセル</button>
                    <button type="submit" class="confirm-btn">確認</button>
                </div>
            </form>
        </div>
    </div>

</div>
</body>
</html>

